// lib/scanner/namePipelineV80.ts
// Phase 8.0 — Name Pipeline V80 (Safe Stub)
// PURPOSE: UNBLOCK BUILD — REAL LOGIC CAN BE FILLED LATER

export function runNamePipelineV80(input: {
  nameFirstPipelineResult: any;
  imageResultsV3: any[];
}) {
  // 🔒 SAFE PASS-THROUGH
  // This preserves existing name resolution without mutation
  return input.nameFirstPipelineResult;
}
