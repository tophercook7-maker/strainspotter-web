// Stub — wiki report sections (planned, not yet implemented)
export type WikiReportSection = { title: string; content: string };
export type WikiReportSections = {
  overview?: WikiReportSection;
  effects?: WikiReportSection;
  terpenes?: WikiReportSection;
  genetics?: WikiReportSection;
  growing?: WikiReportSection;
};
