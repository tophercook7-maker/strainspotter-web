"use client";
// import { useEffect, useState } from "react";

export default function SplashScreen() {
  // Splash animation intentionally disabled until branded video asset exists
  // TODO: Add branded splash animation (v2 branding pass)
  // 
  // const [show, setShow] = useState(true);
  // useEffect(() => {
  //   const timer = setTimeout(() => setShow(false), 2400);
  //   return () => clearTimeout(timer);
  // }, []);
  // if (!show) return null;
  // return (
  //   <div className="fixed inset-0 z-[9999] bg-[#0A1A12] flex items-center justify-center">
  //     <video
  //       src="/brand/animations/splash/splash.mp4"
  //       autoPlay
  //       muted
  //       playsInline
  //       className="w-full h-full object-cover"
  //     />
  //   </div>
  // );

  return null;
}
